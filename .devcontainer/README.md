# Lean development container

Open the repository in GitHub Codespaces, or use **Dev Containers: Reopen in
Container** in VS Code.

The container installs `elan` and the Lean 4 VS Code extension. During initial
setup, it downloads the toolchain pinned by `lean/lean-toolchain` and runs
`lake build` in the `lean` directory.
